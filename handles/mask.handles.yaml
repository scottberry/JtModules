---
version: 0.1.0

input:

    - name: objects
      type: LabelImage
      key:
      help: Image containing objects to be masked

    - name: mask
      type: LabelImage
      key:
      help: Mask to apply to objects

    - name: plot
      type: Plot
      value: false
      help: Should a figure be created?

output:

    - name: masked_image
      type: LabelImage
      key: objects.masked_image
      help: Objects masked and relabeled

    - name: figure
      type: Figure


